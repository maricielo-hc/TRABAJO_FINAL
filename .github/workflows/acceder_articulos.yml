name: Actualizar Articulos

on: 
  schedule:
    - cron: '0 */12 * * *'  # Cada 12 horas
  workflow_dispatch:       # También puede ejecutarse manualmente

jobs:
  actualizar:
    runs-on: ubuntu-latest

    steps:
      - name: Clonar el repositorio
        uses: actions/checkout@v3

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Instalar librerías necesarias 
        run: |
          pip install --upgrade pip
          pip install selenium beautifulsoup4

      - name: Ejecutar el script para actualizar noticias
        run: |
          python scripts/acceder_articulos.py

      - name: Subir cambios si los hay
        run: |
          git config user.name "Dhalia"
          git config user.email "alfarodomini@gmail.com"
          git add js/articulos.js
          git commit -m "🔄 Actualización automática de noticias cada 12h" || echo "Sin cambios"
          git push origin main
